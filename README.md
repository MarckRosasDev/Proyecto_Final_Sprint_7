# Vehicles EDA — Streamlit

Aplicación web simple para explorar anuncios de vehículos (histograma y dispersión) usando **Streamlit** y **Plotly Express**.

##  Demo local

```bash
python -m pip install -r requirements.txt
python -m streamlit run app.py
